# 02. Architecture — システム設計

## 全体構成

E.G.Oは「拡張型モジュラーモノリス + プラグインアーキテクチャ」を採用する。1つのアプリケーションとしてシンプルに動作しながら、内部は機能ごとにモジュール化され、後から機能を追加しやすい構造を持つ。

## レイヤー構成

E.G.Oは4つの層で構成される。

### 入力層 / インターフェース

- Discord、Telegram、CLIなどから入力を受け取る
- Hermes Agentが入力とスキル実行を担う
- ユーザーとの接点となる層

### E.G.O Core（中枢）

意志の保護・正本管理・文脈の蓄積を担う中核。以下のモジュールで構成される。

- **Thought Structurer**：自由記述を要約・課題・選択肢・次のアクションに構造化する（原則1・2）
- **Session Manager**：文脈を前面化し、どの情報を参照するかを制御する
- **Approval Flow**：AIの解釈を人間が承認する仕組み（candidate → active）
- **Source of Truth**：正本を管理し、上書きせず履歴として残す
- **Audit Log**：全操作を記録する

### データ層（4層構造）

| 層 | 役割 |
|---|---|
| Canonical Layer（正本層） | 最終的に信じる情報 |
| Memory Layer（記憶層） | 中長期の記憶 |
| Observation Layer（観測層） | 外部ソースからの観測 |
| Raw Evidence Layer（原文層） | 復元・監査のための元データ |

ストレージの役割分担：

| ストレージ | 役割 |
|---|---|
| PostgreSQL + pgvector | 正本・記憶・メタデータ・全文/ベクトル検索 |
| Valkey | セッションキャッシュ・短期状態 |
| Object Storage | 原文（raw HTML・PDF・会話原文） |

### LLM層（抽象化レイヤー）

特定プロバイダーに依存しない。

| 用途 | プロバイダー | 理由 |
|---|---|---|
| 深い思考・handoff | Claude Pro | 長文・思想的議論に強い |
| 前処理・分類（並列） | cheap LLM | コスト・速度のバランス |
| ローカル実行 | Ollama経由 | プライバシー・オフライン対応 |

## データの状態遷移

正本は直接上書きせず、状態を遷移させる。
```
candidate（候補） → active（有効） → superseded（置換済）

↓

invalid / archived
```
なぜ状態遷移か：
- 「なぜ考えが変わったか」を残せる
- 復元できる状態を保てる
- 誤情報の混入を防げる

これは原則1（意志の文脈を劣化させない）の具体的な実装である。既存のLLMは過去の情報をフラットに扱い、古くなった判断もそのまま参照してしまう。E.G.Oは情報に時間軸を持たせ、常に「今有効な正本」だけを参照できるようにする。

## 設計を貫く4原則との対応

| モジュール | 対応する原則 |
|---|---|
| Thought Structurer | 原則1（原文保持）・原則2（承認前提） |
| Approval Flow | 原則2（LLMは補助役） |
| Source of Truth | 原則1（履歴を残す） |
| LLM抽象化レイヤー | 原則2（プロバイダー非依存） |
| Audit Log | 原則1（全操作の痕跡） |
| プラグイン構造 | 原則3（常駐コンポーネントを増やさない） |

## Phase 1.0での実装範囲

現在の最小実装で動くもの：
- 思考の構造化
- 承認フロー（candidate → active）
- 正本管理（SQLiteで代替）
- 監査ログ

将来のPhaseで実装：
- PostgreSQL + pgvectorへの移行（Phase 1.5）
- 並列cheap LLM処理（Phase 1.5）
- Hermes Agent統合（Phase 1.5）
- AIの解釈過程の可視化（Phase 1.5以降の課題）

Phase 1.0は「思想が機能するか」の検証を目的とする。
